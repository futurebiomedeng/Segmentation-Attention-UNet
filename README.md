# Segmentation-Attention-UNet

TensorFlow implementation of an Attention U-Net model for breast ultrasound image segmentation, evaluated using binary accuracy, Dice coefficient, IoU score, loss, precision, and recall metrics.
